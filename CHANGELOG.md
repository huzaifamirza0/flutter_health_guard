# Changelog

## 0.1.0

- Initial `guardian_core` release
- `Guardian.initialize` / `generateReport` / `dispose` API
- Collectors: crashes, logs, device, lifecycle, navigation, performance, network (IO)
- `GuardianWatch` for widget rebuild tracking
- Analyzer + score engine + recommendations
- Self-contained HTML report + JSON source of truth + CLI summary
